FROM python:3.9-slim

WORKDIR /app

# 安装依赖
COPY scripts/webhook-server.py /app/
RUN pip install flask

# 复制部署脚本
COPY scripts/deploy.sh /app/scripts/
RUN chmod +x /app/scripts/deploy.sh

# 创建日志目录
RUN mkdir -p /app/logs

EXPOSE 8080

CMD ["python", "webhook-server.py"]
